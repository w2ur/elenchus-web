{
  "name": "elenchus-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Static Astro site for elenchus.untilt.app — paste-text reasoning analyzer, calling the elenchus-proxy Cloudflare Worker.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^7.2.6"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "vitest": "^4.1.11"
  }
}
